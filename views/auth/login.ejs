<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Iniciar sesión</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="min-h-screen text-gray-200">
  <%- include('../nav.ejs') %>
  <div class="mt-24 w-full max-w-md mx-auto">
    <h1 class="text-3xl font-extrabold mb-6 text-center tracking-tight title-gradient">Iniciar sesión</h1>
    <% if (error) { %>
      <div class="error bg-red-900/80 text-red-200 rounded-lg px-4 py-2 mb-4 text-center"><%= error %></div>
    <% } %>
    <form method="POST" action="/login" class="space-y-5">
      <label class="block">
        <span class="block mb-1 font-semibold">Nombre de usuario</span>
        <input type="text" name="username" required class="input-dark w-full" placeholder="Tu usuario">
      </label>
      <label class="block">
        <span class="block mb-1 font-semibold">Contraseña</span>
        <input type="password" name="password" required class="input-dark w-full" placeholder="Tu contraseña">
      </label>
      <button type="submit" class="btn-main w-full py-2 rounded-lg font-semibold shadow transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-900">Entrar</button>
    </form>
    <p class="mt-6 text-center text-gray-400">¿No tienes cuenta? <a href="/register" class="text-blue-400 hover:underline">Regístrate</a></p>
  </div>
</body>
</html> 