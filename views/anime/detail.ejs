<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= anime.title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="min-h-screen text-gray-200">
  <%- include('../nav.ejs') %>
  <div class="max-w-2xl mx-auto py-12 px-4">
    <h1 class="text-3xl font-extrabold mb-2 title-gradient"><%= anime.title %></h1>
    <div class="text-xs text-gray-500 mb-6"><%= anime.date %></div>
    <% if (anime.image) { %>
      <img src="<%= anime.image %>" alt="Imagen de la noticia" class="mb-6 rounded-lg max-w-full mx-auto" style="max-height:350px;">
    <% } %>
    <p class="text-lg leading-relaxed text-gray-200 mb-8"><%= anime.content %></p>
    <div class="text-center">
      <a href="/" class="btn-main inline-block px-5 py-2 rounded-lg font-semibold shadow transition-all duration-200 focus:outline-none focus:ring-2 focus:ring-blue-900">Volver a la lista</a>
    </div>
    <% if (typeof user !== 'undefined' && user.isAdmin) { %>
      <form method="POST" action="/news/<%= anime.id %>/delete" onsubmit="return confirm('¿Seguro que quieres borrar esta noticia?');">
        <button type="submit" class="btn-main mt-4 px-4 py-2 rounded-md font-semibold">Borrar noticia</button>
      </form>
    <% } %>
  </div>
  <%- include('../footer.ejs') %>
</body>
</html> 